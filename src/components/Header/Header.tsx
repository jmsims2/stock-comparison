import React from "react";
import { Pane, Heading } from "evergreen-ui";

export default function Header() {
    return (
        <Pane padding={16}>
            <Heading size={900}>Stock Comparison</Heading>
        </Pane>
    );
}
